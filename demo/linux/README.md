# Bundled Linux browser guest

`Image` and `rootfs.cpio` are generated by running `./scripts/build-linux.sh`
from the repository root. They use the pinned Buildroot and Linux versions in
the external tree under `buildroot/` and are bundled so the default browser
preset boots to a root login prompt without requiring uploads.
